# RELAY — instructions from the user

Claude: **read this whole file before starting any new research action** (new training run, new experiment, new direction). Act on whatever the user has written below. If nothing new has been added since you last acted, you have **full autonomy** to proceed per the standing rules + A/B plan.

The user will write instructions here **tonight**.

---

## USER INSTRUCTIONS / INBOX

- **Codex setup 2026-07-10 — Tonight 2-hour garment-net training plans are in `bgsmearresearch/TONIGHT_GARMENT_NET_RUNS.md`.**
  - **Goal:** run sequential 2-hour probes to find a real Qwen-block garment-net recipe. A 2-hour probe must show a real signal before any long run is justified. Do not start long training tonight unless the user explicitly asks.
  - **Main objective:** improve garment identity/details while preserving run37 background and skin. The garment net should supply missing garment detail signal to the frozen run37 denoiser; it is not a second try-on generator.
  - **Important plumbing patch already made:** OOTD garment branch now respects `_XATTN_BYPASS`, and the bridge wrong-garment path can swap OOTD garment features. Use this before training OOTD; do not train blind if branch OFF / branch ON / wrong-garment forwards are not all working.
  - **Run order:** run63 OOTD Qwen-block branch under bridge, run64 OOTD injection timing probe, run65 pose/layout-aware garment memory, run66 target-space garment detail memory, run67 detail/high-pass branch, run68 prior-starvation probe. If any run passes, run one 2-hour confirmation with tighter preservation; only then write the long-run recipe.
  - **Hard rejects:** no entropy/coverage main lever, no full global GarmentChain hidden add, no small U-Net/conv refiner as the garment-net answer, no accepting `wrong ~= correct`, no output-space refiner claim as an in-loop garment-net win, no fixed bg-band/chroma/luminance soup.
  - **Eval:** fixed IDs `00006_00,00008_00,00013_00,00017_00,00034_00`; branch OFF vs ON; correct/wrong/zero/shuffled garment sensitivity; canonical raw `predict_sample` CLOUD/far_bg/whole_bg/garL1/skinL1/PSNR/SSIM; actual panels inspected before any win claim.

- **Codex review 2026-07-06 11:32 — Reviewed newest folder `run25_fg_lr`. Verdict: PENDING / TRAINING IN PROGRESS; bg smear/darkening cannot be called gone, improved, unchanged, or worse yet.**
  - Evidence: `bgsmearresearch/run25_fg_lr/` is empty, `/tmp/eval_run25*` does not exist, and there are no README/RESULTS/panels/results yet. Matching run is `runs/BGBAND25_104621` / `logs/BGBAND25_104621.log`; it currently has only `config.json`/`train.log`, no `latest/`, no `best_val/`, no `final/`. Host process list shows `/home/link/venvs/ootd/bin/python -u train.py` still running.
  - This item supersedes the older 09:33 run24-pending instruction below. Run24 is now documented and evaluated; run25 is the active newest run.
  - Completed run24 context: bg stayed locked but foreground got worse. Metrics vs run14 / run23 / run24: `CLOUD 3.34 / 3.31 / 3.34`, `edit_bg_dL -1.28 / -1.25 / -1.30`, `garL1 19.12 / 18.34 / 19.36`, `skinL1 19.63 / 19.62 / 19.79`. Verdict: larger 1024 FG head erased run23's garment gain; capacity is not the lever.
  - Design check for run25: one-lever LR ablation on the run23-style 512 nonlinear FG heads: base `LR 2e-5→5e-5`, v6 heads lr logged as `5e-4`; run14 base frozen; `FREEZE_LORA=1`, `FREEZE_MULTI_BLOCK=1`, `USE_V6_REFINE=1`, `V6_FG_NONLINEAR=1`, `V6_FG_HIDDEN=512`, `V6_TRAIN_FG_ONLY=1`, `V6_BG_LOCK=1`; log confirms `to_b + to_route` frozen and fixed bg losses off. This is rule-compatible as a final training-limited-vs-feature-ceiling probe, but it is not the region-routed adapter direction and must not spawn more frozen-head variants.
  - Current training/val/in-loop output is not the acceptance metric. Do not claim success or failure from training loss, `val_img`, or `[DEPLOY]`.
  - **NEXT exact action:** do not start run26 or any new experiment. Let run25 finish to `final/` under the 2h budget policy, or explicitly select `latest/`/`best_val/` only if the run is intentionally stopped. Then immediately run the same fixed 5-ID deployed eval and populate `bgsmearresearch/run25_fg_lr/` with README, RESULTS, copied `results.json`, raw `pred_vs_gt`, final/deployed `final_vs_gt`, heatmaps, and an `EXPERIMENT_LEDGER.md` run25 entry.
  - **Decision rule after eval:** accept run25 only if bg remains run14/run23-level in metrics and actual panels while garment and/or skin clearly improve over run23. If bg moves materially away from run23, classify as an implementation leak despite `V6_BG_LOCK`. If bg is held but garment/skin do not beat run23, conclude frozen-feature FG heads have plateaued.
  - **Next experiment only if run25 plateaus with bg locked:** run `run26_region_routed_fg_adapters`: parent run14/run23; keep bg path frozen/locked; add true parameter-separated foreground adapters or region-routed LoRA active only for garment/skin regions; compose/mask adapter effect out of bg in the same deploy-faithful eval path. Do not start this until run25 eval/docs/images exist.
  - **DO NOT:** start run26 before run25 eval/docs/ledger; run more frozen-head capacity/LR variants after run25; claim anything from val loss or in-loop deploy logs; train/apply `to_b`; unfreeze shared LoRA/main network without region isolation; add scalar foreground tuning; reduce bg share; push `W_IMG_V6_B`; add fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA`; use soup targets; let foreground/global losses update or overwrite bg.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`/matched refine eval; report CLOUD, `edit_bg_dL`, `edit_nonbg_dL`, `edit_all_dL`, `whole_bg_dL`, `far_bg_dL`, garL1, skinL1, edit PSNR/SSIM. Compare directly against run14, run23, run24, and the run23 zero-head/run14 reproduction. Inspect actual panels before claiming any win.
  - **Rules enforced:** true bifurcation, no soup, lower-level priority, deployed-quality metrics, keep the loss stack simple.

- **Codex review 2026-07-06 09:33 — Reviewed newest folder `run24_fg_capacity`. Verdict: PENDING / TRAINING IN PROGRESS; bg smear/darkening cannot be called gone, improved, unchanged, or worse yet.**
  - Evidence: `bgsmearresearch/run24_fg_capacity/` is empty, `/tmp/eval_run24*` does not exist, and there are no fixed 5-ID result panels or `results.json`. Matching checkpoint/log is `runs/BGBAND24_083437` / `logs/BGBAND24_083437.log`; it currently has `latest/` and `best_val/`, no `final/`. Host process list shows `/home/link/venvs/ootd/bin/python -u train.py` still running.
  - This item supersedes the older 07:32 run23-pending instruction below; run23 is now documented and run24 is already the active newest run.
  - Design check: run24 is the intended one-lever scale-up from run23: `V6_FG_HIDDEN 512→1024`, with run14 base frozen, `FREEZE_LORA=1`, `FREEZE_MULTI_BLOCK=1`, `USE_V6_REFINE=1`, `V6_FG_NONLINEAR=1`, `V6_TRAIN_FG_ONLY=1`, `V6_BG_LOCK=1`, and log-confirmed `to_b + to_route` frozen. Fixed bg losses remain off. This is rule-compatible as a pending capacity probe, not a result.
  - Run23 context: run23 is the current valid separation reference, not run24. It held bg at run14 level (`CLOUD 3.34→3.31`, `edit_bg_dL -1.28→-1.25`) while modestly improving garment (`garL1 19.12→18.34`). Run24 must beat run23 on foreground while preserving that bg lock.
  - Current in-loop `[DEPLOY]` at step 805 is not the acceptance metric. Do not use it as the verdict, and do not claim success or failure from training loss.
  - **NEXT exact action:** do not start run25 or any new experiment. Let run24 finish to `final/` under the 2h budget policy, or explicitly select `latest/`/`best_val/` only if the run is intentionally stopped. Then immediately run the same fixed 5-ID deployed eval and populate `bgsmearresearch/run24_fg_capacity/` with README, RESULTS, copied `results.json`, raw `pred_vs_gt`, final/deployed `final_vs_gt`, heatmaps, and an `EXPERIMENT_LEDGER.md` run24 entry.
  - **Decision rule after eval:** accept run24 only if bg remains run14/run23-level in metrics and actual panels while garment and/or skin improve over run23. If `CLOUD` or `edit_bg_dL` moves materially away from run23, classify it as an implementation leak despite `V6_BG_LOCK`. If bg is held but garment/skin do not improve, classify it as `frozen-feature foreground capacity plateau`.
  - **Next experiment only if run24 plateaus with bg locked:** run `run25_region_routed_fg_adapters`: parent run14/run23, keep bg path frozen/locked, add true parameter-separated foreground adapters or region-routed LoRA active only for garment/skin regions, and do not update shared bg-affecting parameters. Do not start this until run24 eval/docs/images exist.
  - **DO NOT:** start run25 before run24 eval/docs/ledger; claim anything from val loss or in-loop deploy logs; train/apply `to_b`; unfreeze shared LoRA/main network as a shortcut; add scalar foreground tuning; reduce bg share; push `W_IMG_V6_B`; add fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA`; use soup targets; let foreground/global losses update or overwrite bg.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`/matched refine eval; report CLOUD, `edit_bg_dL`, `edit_nonbg_dL`, `edit_all_dL`, `whole_bg_dL`, `far_bg_dL`, garL1, skinL1, edit PSNR/SSIM. Compare directly against run14, run21, run23, and the run23 zero-head/run14 reproduction. Inspect actual panels before claiming any win.
  - **Rules enforced:** true bifurcation, no soup, lower-level priority, deployed-quality metrics, keep the loss stack simple.

- **Codex review 2026-07-06 07:32 — Reviewed newest folder `run23_bgfrozen_fg_nonlinear`. Verdict: PENDING / TRAINING IN PROGRESS; bg smear/darkening cannot be called gone, improved, unchanged, or worse yet.**
  - Evidence: `bgsmearresearch/run23_bgfrozen_fg_nonlinear/` currently has no `README.md`, no `RESULTS.md`, no panels, and no copied `results.json`. `/tmp/eval_run23*` does not exist. Matching checkpoint/log exists at `runs/BGBAND23_062251` / `logs/BGBAND23_062251.log`, with `latest/` and `best_val/` but no `final/` yet. Host process list shows `/home/link/venvs/ootd/bin/python -u train.py` still running for this run.
  - Design check: `run_bgband23.sh` follows the intended probe: parent run14; `FREEZE_LORA=1`, `FREEZE_MULTI_BLOCK=1`, `USE_V6_REFINE=1`, `V6_FG_NONLINEAR=1`, `V6_TRAIN_FG_ONLY=1`, `V6_BG_LOCK=1`; log confirms `LoRA FROZEN`, `multi_block_inj FROZEN`, and `V6_TRAIN_FG_ONLY: froze to_b + to_route (bg immovable = run14; train only to_g/to_s)`. Fixed bg losses remain off. This is rule-compatible as a pending probe.
  - Current in-loop `[DEPLOY]` at step 824 is not the acceptance metric. Do not use it as the verdict. Required fixed raw/refine eval and panels are still missing.
  - **NEXT exact action:** do not start run24 or any new experiment. Let run23 finish to `final/` or explicitly stop only if the 2h budget policy says to stop; then immediately run the same fixed 5-ID deployed eval on the selected checkpoint and populate `bgsmearresearch/run23_bgfrozen_fg_nonlinear/` with README, RESULTS, copied `results.json`, raw `pred_vs_gt`, final/deployed `final_vs_gt`, heatmaps, and an `EXPERIMENT_LEDGER.md` run23 entry.
  - **Decision rule after eval:** accept only if bg remains run14-level (`CLOUD` and `edit_bg_dL` materially unchanged from run14) while garment and/or skin improve in metrics and actual panels. If bg moves away from run14, classify as implementation leak despite `V6_BG_LOCK`. If bg is locked but garment/skin do not improve, classify as `frozen-feature foreground capacity insufficient`.
  - **Next experiment only if run23 fails with bg locked:** run `run24_region_routed_fg_adapters`: parent run14; keep bg path frozen/locked; add true parameter-separated foreground adapters/LoRA path active only for garment/skin regions; do not update shared bg-affecting parameters. Do not start this until run23 eval/docs/images exist.
  - **DO NOT:** start run24 before run23 eval/docs/ledger; claim success from val loss or in-loop deploy logs; run GPU eval before training completion unless explicitly selecting `latest`/`best_val`; train/apply `to_b`; unfreeze shared LoRA/main network; add scalar foreground tuning; reduce bg share; push `W_IMG_V6_B`; add fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA`; use soup targets; let foreground/global losses update or overwrite bg.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`/matched refine eval; report CLOUD, `edit_bg_dL`, `edit_nonbg_dL`, `edit_all_dL`, `whole_bg_dL`, `far_bg_dL`, garL1, skinL1, edit PSNR/SSIM. Compare directly against run14, run21, and run23 zero-head/run14 reproduction if available. Inspect actual panels before claiming any win.
  - **Rules enforced:** true bifurcation, no soup, lower-level priority, deployed-quality metrics, keep the loss stack simple.

- **Codex review 2026-07-06 05:29 — Newest documented run is still `run21_region_residual_heads`. Verdict unchanged: REGRESSED / REJECTED; bg smear/darkening is not gone.**
  - No newer `bgsmearresearch/runNN_*` folder, `BGBAND*` checkpoint/log, or `/tmp/eval_run*` exists after run21. `EXPERIMENT_LEDGER.md` still has no run21 entry, so fix the ledger before starting another experiment.
  - Metric verdict vs run14 remains: CLOUD **3.34 → 7.19**, `edit_bg_dL` **-1.28 → +7.02**, garL1 **19.12 → 21.63**, skinL1 **19.63 → 20.40**, edit PSNR/SSIM **19.88/0.831 → 19.37/0.807**. This is a bright-halo regression, not a bg fix.
  - Visual verdict remains worse: the saved panels for `00006`, `00013`, and `00034` show a visible white/bright ring around the generated edit background. The artifact changed sign from dark to bright, but it is still a background defect.
  - **NEXT exact action:** first add a complete `run21_region_residual_heads` entry to `EXPERIMENT_LEDGER.md`. Then run `run23_bgfrozen_fg_nonlinear`: parent run14; bg path/output locked exactly to run14; no trainable bg residual; train only small nonlinear foreground capacity for garment/skin through the deploy-faithful refine path, e.g. separate 2-layer bottleneck MLP or 1x1-conv residual heads masked only to garment/skin.
  - **Why this next:** run21 proved the refine hook can reproduce run14 when residuals are zero, so deployment plumbing is real. It also proved linear frozen-feature heads are not enough and trainable `to_b` overshoots. The next probe should isolate foreground capacity while making bg immovable, not jump straight to shared full-network unfreeze.
  - **DO NOT:** follow run21 README's broad "unfreeze FULL network / masked gradient routing" recommendation yet; continue linear residual heads; train or apply `to_b`; adopt run21; start any scalar foreground tuning; reduce bg share; push `W_IMG_V6_B`; add fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA`; use soup targets; let foreground/global losses update or overwrite bg.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`/matched refine eval; save raw `pred_vs_gt`, final/deployed `final_vs_gt`, and heatmap panels in the run folder; report CLOUD, `edit_bg_dL`, `edit_nonbg_dL`, `edit_all_dL`, `whole_bg_dL`, `far_bg_dL`, garL1, skinL1, edit PSNR/SSIM. Compare directly against run14 and run21. Inspect actual panels before claiming any win.
  - **Rules enforced:** true bifurcation, no soup, lower-level priority, deployed-quality metrics, keep the loss stack simple.

- **Codex review 2026-07-06 03:29 — Reviewed `run21_region_residual_heads`. Verdict: REGRESSED / REJECTED; bg smear/darkening is not gone.**
  - Metric verdict vs run14: CLOUD **3.34 → 7.19**, `edit_bg_dL` **-1.28 → +7.02**, `edit_nonbg_dL` **-2.91 → -4.85**, `edit_all_dL` **-2.37 → -1.21**, garL1 **19.12 → 21.63**, skinL1 **19.63 → 20.40**, edit PSNR/SSIM **19.88/0.831 → 19.37/0.807**. This fails the bg gate and also fails foreground quality.
  - Visual verdict: `run21_region_residual_heads/00006_pred_final_gt.png`, `00013_pred_final_gt.png`, and `00034_pred_final_gt.png` show a clear bright/white halo around the person/edit background band. This is not the original dark smear, but it is still a deployed background artifact and is visually worse than run14.
  - Design lesson: the zero-head self-check is useful: the refine eval can reproduce run14 exactly, so the hook/compose/decode plumbing is valid. The failure is the model choice: trainable **linear** region residual heads on frozen run14 features do not improve garment/skin and the trainable bg head overshoots. Do not use run21 as a baseline.
  - **NEXT exact experiment:** run `run23_bgfrozen_fg_nonlinear`. Parent checkpoint: run14. Keep the run14 bg path/output frozen exactly. Do **not** train `to_b` and do not apply any trainable residual in edit-bg pixels. Train only a small nonlinear foreground module for garment/skin, e.g. separate 2-layer bottleneck MLP or 1x1-conv residual heads for garment and skin, composed only by garment/skin masks through the same deploy-faithful refine path. Purpose: test whether extra foreground capacity can improve garment/skin while bg remains numerically locked to run14.
  - **Gate for this next run:** if CLOUD or `edit_bg_dL` moves materially away from run14, treat it as an implementation leak. If bg remains run14-level but garment/skin do not improve, conclude frozen-feature foreground heads are insufficient and only then consider a region-routed adapter/LoRA path with true parameter separation.
  - **DO NOT:** continue run21; adopt run21; train the bg residual head; unfreeze the full shared LoRA/main network as the next step; run broad "masked gradient routing" on shared parameters yet; add scalar foreground tuning; reduce bg share; push `W_IMG_V6_B`; add fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA`; use soup targets; let foreground/global losses overwrite bg.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`/matched refine eval; save raw `pred_vs_gt`, final/deployed `final_vs_gt`, and heatmap panels in the run folder; report CLOUD, `edit_bg_dL`, `edit_nonbg_dL`, `edit_all_dL`, `whole_bg_dL`, `far_bg_dL`, garL1, skinL1, edit PSNR/SSIM; update `EXPERIMENT_LEDGER.md`. Compare directly against run14 and run21. Inspect actual panels before claiming any win.
  - **Rules enforced:** true bifurcation, no soup, lower-level priority, deployed-quality metrics, keep the loss stack simple.

- **Codex review 2026-07-06 01:30 — Reviewed newest folder `run21_region_residual_heads`. Verdict: PENDING / NOT EVALUATED; bg smear/darkening cannot be called gone, improved, unchanged, or worse yet.**
  - Evidence: `bgsmearresearch/run21_region_residual_heads/` is empty: no `README.md`, no `RESULTS.md`, no image panels, and no ledger entry. `/tmp/eval_run21` does not exist. `runs/BGBAND21_234151/` has `latest/` and `best_val/`, but no `final/` and no fixed 5-ID eval artifacts.
  - Design seen in `run_bgband21.sh`: warm-start from run14, `FREEZE_LORA=1`, `FREEZE_MULTI_BLOCK=1`, `FREEZE_V6=0`, `USE_V6_REFINE=1`; intended probe is frozen run14 base plus trainable region residual heads. Log confirms `LoRA FROZEN`, `multi_block_inj FROZEN`, v6 heads trainable, and `USE_V6_REFINE` zeroed residual heads so the start equals run14. This is the right structural direction, but it is not a result until deployment eval exists.
  - In-loop `[DEPLOY]` at steps 798 and 1570 is not the acceptance metric and should not be used as the verdict. It reports BG dim around `-8.87` / L1 `18.90`, but the required fixed `predict_sample` CLOUD eval is missing.
  - **NEXT exact action:** do not start run22 or any new experiment. Finish `run21_region_residual_heads`, then immediately run the standard fixed 5-ID raw `predict_sample` eval on the selected checkpoint (`final` if created; otherwise explicitly justify `latest` or `best_val`). Populate `bgsmearresearch/run21_region_residual_heads/` with README, RESULTS, copied `results.json`, raw/final panels, heatmaps, and an `EXPERIMENT_LEDGER.md` entry.
  - **Eval requirement:** same IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`; report CLOUD, `edit_bg_dL`, `edit_nonbg_dL`, `edit_all_dL`, `whole_bg_dL`, `far_bg_dL`, garL1, skinL1, edit PSNR/SSIM. Compare directly against run14, run18, run19, and run20. Inspect actual panels before claiming anything.
  - **Verdict rule:** accept run21 only if bg/ring is no worse than run14 while garment and/or skin improves. If foreground improves but CLOUD or `edit_bg_dL` regresses, classify it as `foreground/bg tradeoff, not separation`. If region heads do not affect deployed output, classify it as `structural path not deployed/effective`.
  - **DO NOT:** start another run before run21 eval/docs/images/ledger are complete; claim success from val loss or in-loop deploy logs; add scalar foreground tuning; reduce bg share; push `W_IMG_V6_B` above 8; add fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA`; use soup targets; let foreground/global losses overwrite bg.
  - **Rules enforced:** true bifurcation, no soup, lower-level priority, deployed-quality metrics, keep the loss stack simple.

- **User/Codex overnight instruction 2026-07-05 23:00 — Stop scalar foreground tuning; run 2h structural region-separation probes and log every experiment.**
  - Current accepted checkpoint remains **run14** (`runs/BGBAND14_033138/final`). Run18, run19, and run20 are rejected but informative: all improved garment/skin and all worsened bg. This proves scalar foreground strengthening is trading along the coupled frontier, not learning true region separation.
  - Run14 reference gate: CLOUD **3.34**, `edit_bg_dL` **-1.28**, `edit_all_dL` **-2.37**, garL1 **19.12**, skinL1 **19.63**. A new run cannot be accepted unless bg/ring is no worse than run14 while garment and/or skin improves.
  - **Overnight goal:** find a setup that structurally separates bg / garment / skin so foreground quality can improve without re-darkening the run14 background. Do not run long-form baseline yet. Every experiment is a 2h probe.
  - **Required experiment ledger:** create/update `vtonautoresearch/bgsmearresearch/EXPERIMENT_LEDGER.md` after every run. For every run record: run number/name, launcher, parent checkpoint, exact single hypothesis, exact changed files/modules/env vars, start/end time, checkpoint path, eval path, metrics vs run14, panel path, verdict, and lesson. Do not rely on memory.
  - **Probe queue, one design per run:** first try a minimal structural separation design, not another scalar weight tweak. Preferred first probe: region-specific residual heads or adapters for bg / skin / garment, composed by masks, with bg path initialized/preserved from run14 behavior and foreground path allowed to improve garment/skin. If that is too invasive for one 2h probe, use the smallest deploy-faithful version that proves whether region-specific parameters can affect only their region.
  - Acceptable structural directions: separate region residual heads composed by masks; region-routed LoRA/adapters; bg-frozen path plus trainable foreground path; masked gradient routing that prevents foreground losses from updating bg route/head parameters; split multi-block garment/bg branches with explicit region gates. Pick one per run.
  - **Do not:** continue scalar-only tuning (`W_IMG_V6_G_ID`, `W_IMG_V6_S`, `MULTI_BLOCK_LR`, `RPCT_SKIN`) as the main plan; combine many levers; reduce bg share below run14; push `W_IMG_V6_B` above 8; add fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA`; use soup targets; let foreground/global losses overwrite bg; claim success from training loss or pasted panels alone.
  - **Eval requirement for every run:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`; report CLOUD, `edit_bg_dL`, `edit_nonbg_dL`, `edit_all_dL`, `whole_bg_dL`, `far_bg_dL`, garL1, skinL1, edit PSNR/SSIM; save raw `pred_vs_gt`, final/deployed `final_vs_gt`, and edit-region dL heatmap/panel inside that run folder. Compare directly to run14, run18, run19, run20.
  - **Verdict rule:** if garment/skin improves but CLOUD or `edit_bg_dL` regresses, label it `foreground/bg tradeoff, not separation`, log it, and do not adopt it. A true win must preserve the run14 bg while improving foreground in metrics and actual panels.
  - **Rules enforced:** true bifurcation, no soup, lower-level priority, deployed-quality metrics, keep loss stack simple.

- **Codex review 2026-07-05 13:25 — Reviewed newest folder `run17_win_plus_garskin`. Verdict: REGRESSED / NOT ACCEPTED; bg smear/darkening is not gone.**
  - Evidence: `run17_win_plus_garskin/README.md` and `/tmp/eval_run17/results.json` now exist, with panel `/tmp/eval_run17/panel_all.png`. The panel still shows a darker generated edit region and strong residual heatmap structure over the agnostic/edit band.
  - Metric verdict vs run14: CLOUD **3.34 → 4.54**, `edit_bg_dL` **−1.28 → −3.01**, `edit_nonbg_dL` **−2.91 → −5.75**, `edit_all_dL` **−2.37 → −4.80**, garL1 **19.12 → 22.02**, whole-bg dL **−10.51 → −17.91**, far-bg dL **−17.12 → −31.44**. SkinL1 improves slightly **19.63 → 18.84**, but the deployed background/edit quality regresses materially.
  - Design lesson: run14's useful lever was **bg-region image weight 8 by itself**. Raising garment/skin image weights at the same time counteracts that benefit and re-darkens the edit hole. Run14 remains the best current candidate; run17 must not become the baseline.
  - **NEXT exact experiment:** run `run18_confirm_bgweight_w8`: exact repeat of run14 from run03 with only `W_IMG_V6_B=8.0` changed. Keep garment/skin image weights at run14/default values, keep all fixed bg luminance/field/chroma losses at `0.0`, keep the same run03 shares, same architecture, same SOAR setup, same noise-fill agnostic setup, and no conditioning changes. Save README, RESULTS, raw/final panels, and heatmaps inside `bgsmearresearch/run18_confirm_bgweight_w8/`, not only `/tmp`.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`; report raw CLOUD, signed darkening, `edit_bg_dL`, `edit_all_dL`, `edit_nonbg_dL`, `whole_bg_dL`, `far_bg_dL`, garment L1, skin L1, edit PSNR/SSIM; save raw `pred_vs_gt`, final/deployed `final_vs_gt`, and edit-region dL heatmap panels. Compare against run03, run14, run15, run16, run17, soar 8.19, and if5 ~3.4. Accept only if actual panels visibly improve and garment/skin do not materially regress.
  - **DO NOT:** continue `run17_win_plus_garskin`; raise garment/skin weights together with bg; push `W_IMG_V6_B` above 8; run agnostic-inpaint/conditioning changes; add/tune fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA`; add bg mean/field targets; change SOAR at the same time; change multiple independent levers; use soup losses; let bg/global terms overwrite garment/skin; score grey-hole/deployed paste alone.
  - **Rules enforced:** true bifurcation via existing detach-normalized region shares, no soup, lower-level priority for garment/skin, deployed-quality metrics, and a simple one-lever loss stack.

- **Codex review 2026-07-05 09:19 — Reviewed newest folder `run16_bgweight_push`. Verdict: PENDING / UNDOCUMENTED / DO NOT CONTINUE; bg smear/darkening is not proven improved or gone.**
  - Evidence: `run16_bgweight_push/` contains only `run03_baseline_results.json` and `run14_best_results.json`. There is no `README.md`, no `RESULTS.md`, no image panels, no `/tmp/eval_run16` artifacts, no matching `BGBAND16` checkpoint/log, and no active `BGBAND16` process. Do not claim a run16 result.
  - Design issue: `run_bgband16.sh` pushes `W_IMG_V6_B` from 8 to **11** before the required W=8 confirmation exists. That violates the previous gate. Run14 is promising, but it is one run with slight skin regression; it must be confirmed before pushing the lever harder.
  - Additional context: `run15_agnostic_inpaint` was a conditioning change before confirmation and it failed the deploy gate. Metrics from its README: CLOUD **3.28 → 5.70**, `edit_bg_dL` **−1.31 → −6.09**, `edit_all_dL` **−3.78 → −6.60**. Panel `/tmp/eval_run15/panel_all.png` confirms the deployed edit hole is darker/worse despite raw far-bg improvement. Do not pursue agnostic-inpaint/bg-authority for this defect now.
  - **NEXT exact experiment:** run `run17_confirm_bgweight_w8`: exact repeat of run14 from run03 with only `W_IMG_V6_B=8.0` changed, all fixed bg luminance/field/chroma losses at `0.0`, same run03 shares, same architecture, same SOAR setup, same noise-fill agnostic setup, and no conditioning changes. Save README, RESULTS, raw/final panels, and heatmaps inside `bgsmearresearch/run17_confirm_bgweight_w8/`, not only `/tmp`.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`; report raw CLOUD, signed darkening, `edit_bg_dL`, `edit_all_dL`, `edit_nonbg_dL`, `whole_bg_dL`, `far_bg_dL`, garment L1, skin L1, edit PSNR/SSIM; save raw `pred_vs_gt`, final/deployed `final_vs_gt`, and edit-region dL heatmap panels. Compare against run03, run14, run15, soar 8.19, and if5 ~3.4. Accept only if panels visibly improve and skin does not regress further.
  - **DO NOT:** run or continue `run16_bgweight_push`/`W_IMG_V6_B=11` before W=8 is confirmed; run more agnostic-inpaint or other conditioning changes; add/tune fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA`; add bg mean/field targets; change SOAR at the same time; change multiple independent levers; use soup losses; let bg/global terms overwrite garment/skin; score grey-hole/deployed paste alone.
  - **Rules enforced:** true bifurcation via existing detach-normalized region shares, no soup, lower-level priority for garment/skin, deployed-quality metrics, and a simple one-lever loss stack.

- **Codex review 2026-07-05 07:19 — Reviewed `run14_bgweight`. Verdict: IMPROVED / CURRENT BEST CANDIDATE, but bg smear/darkening is not fully gone.**
  - Metric: fixed 5-ID raw eval exists at `/tmp/eval_run14/results.json`. Run14 improved the deploy-relevant edit patch vs run03: CLOUD **3.28 → 3.34** (~flat, near if5 target), `edit_bg_dL` **−1.31 → −1.28** (flat), `edit_nonbg_dL` **−5.02 → −2.91**, `edit_all_dL` **−3.78 → −2.37**, garL1 **22.01 → 19.12**, edit PSNR **19.52 → 19.88**. SkinL1 regressed slightly **18.23 → 19.63**.
  - Visual: `/tmp/eval_run14/panel_all.png` looks better than the bg-luminance-loss runs and the generated edit patch is less dark, especially on 00013/00017/00034. The defect is not gone: there is still residual halo/dL structure, and skin needs protection.
  - Design lesson: bg luminance/field losses in run12/run13 improved raw far-bg brightness while hurting the deploy hole. Run14's lower-level GT reconstruction weighting improved the actual deployed edit patch. Treat run14 as the new best candidate, but confirm before building on it.
  - **NEXT exact experiment:** run `run15_confirm_bgweight`: exact repeat of run14 from run03 with only `W_IMG_V6_B=8.0` changed, all fixed bg luminance/field/chroma losses at `0.0`, same run03 shares/architecture/SOAR setup, and no conditioning changes. Purpose: determine whether the run14 win is reproducible and whether skinL1 regression is noise or a real tradeoff. Copy final panels/results into `bgsmearresearch/run15_confirm_bgweight/`, not only `/tmp`.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`; report raw CLOUD, signed darkening, `edit_bg_dL`, `edit_all_dL`, `edit_nonbg_dL`, `whole_bg_dL`, `far_bg_dL`, garment L1, skin L1, edit PSNR/SSIM; save raw `pred_vs_gt`, final/deployed `final_vs_gt`, and edit-region dL heatmap panels. Compare against run03, run14, run12/run13, soar 8.19, and if5 ~3.4. The confirmation is accepted only if panels still visibly improve and skin does not regress further.
  - **DO NOT:** start `run15_agnostic_inpaint` or any conditioning change before confirming run14; increase `W_IMG_V6_B` above 8 yet; add/tune fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA`; add new bg mean/field targets; change SOAR at the same time; change multiple independent levers; use soup losses; let bg/global terms overwrite garment/skin; score grey-hole/deployed paste alone.
  - **Rules enforced:** true bifurcation via existing detach-normalized region shares, no soup, lower-level priority for garment/skin, deployed-quality metrics, and a simple one-lever loss stack.

- **Codex review 2026-07-05 05:17 — Reviewed `run13_lowfreq_bgmean`. Verdict: REGRESSED / NOT ACCEPTED; bg smear/darkening is not gone.**
  - Metric: fixed 5-ID raw eval exists at `/tmp/eval_run13/results.json`. Run13 worsened the deploy-relevant metrics vs run03: CLOUD **3.28 → 4.24**, `edit_bg_dL` **−1.31 → −2.27**, `edit_all_dL` **−3.78 → −5.90**, and garL1 **22.01 → 23.54**. It improved raw whole/far bg brightness (`far_bg_dL` −36.65 → −14.87), but the generated hole got darker and the ring metric got worse.
  - Visual: `/tmp/eval_run13/panel_all.png` shows the same pattern as run12. The raw far background is cleaner/brighter, but the deployed/edit patch still has a darker generated hole and strong residual structure around person/garment. Do not treat this as an improvement.
  - Design lesson: run12 `LAMBDA_BG_FIELD` and run13 `LAMBDA_BG_CHROMA` both push raw far-bg brightness in the right direction while worsening deploy-hole quality. Stop treating this as a simple bg-luminance-loss problem.
  - **NEXT exact experiment:** replace the current `run14_bgweight` plan. Run `run14_soar_off_ablation`: warm-start from run03, keep run03 architecture, shares, and loss stack unchanged, set all fixed bg luminance/field/chroma losses to `0.0`, and change only one lever: disable SOAR training (`USE_SOAR=0`) for the 2h fine-tune. The purpose is to test whether SOAR/rollout training is driving the deploy-hole darkening/garment-skin interaction while preserving the best run03 bg-share setup.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`; report raw CLOUD, signed darkening, `edit_bg_dL`, `edit_all_dL`, `edit_nonbg_dL`, `whole_bg_dL`, `far_bg_dL`, garment L1, skin L1, edit PSNR/SSIM; save raw `pred_vs_gt`, final/deployed `final_vs_gt`, and edit-region dL heatmap panels. Compare against run03, run12, run13, soar 8.19, and if5 ~3.4. The run is not accepted unless the panels visibly improve.
  - **DO NOT:** run `run_bgband14.sh` as a bg-weight-only increase (`W_IMG_V6_B 5→8`); add/tune fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA`; add a new bg mean/field target; change conditioning at the same time; change multiple independent levers; use soup losses; let rollout/global terms overwrite garment/skin; score grey-hole/deployed paste alone.
  - **Rules enforced:** true bifurcation via detach-normalized shares, no soup, lower-level priority, deployed-quality metrics, and a simple one-lever loss stack.

- **Codex review 2026-07-05 03:17 — Reviewed newest folder `run13_lowfreq_bgmean`. Verdict: PENDING / UNDOCUMENTED / DO NOT RUN AS-IS; bg smear/darkening is NOT proven improved or gone.**
  - Evidence for run13: the folder contains only `run03_baseline_results.json`. There is no `README.md`, no `RESULTS.md`, no image panels, no matching `BGBAND13` checkpoint/log directory, no `/tmp/eval_run13` artifacts, and no active `BGBAND13` process. Do not claim a run13 result.
  - Last substantive result: `run12_bgfield_anchor` finished but is **not accepted over run03**. Metrics: CLOUD **3.28 → 4.38** worse, `edit_bg_dL` **−1.31 → −2.73** darker, `edit_all_dL` **−3.78 → −5.81** darker. It did improve raw far/whole background darkening (`far_bg_dL` −36.65 → −11.72), but the deploy-relevant generated hole got darker/worse. Panels at `/tmp/eval_run12/panel_all.png` do not show the defect gone.
  - Design warning: current `run_bgband13.sh` sets `LAMBDA_BG_CHROMA=2.0`. Do **not** run that as a fixed-lambda bg-mean patch. Fixed-weight bg losses violate the standing true-bifurcation rule, and an agnostic/visible-bg mean target can become soup if it competes with the real-GT bg objective.
  - **NEXT exact experiment:** replace the current run13 plan with `run13_pct_lowfreq_bg_gt`: warm-start from run03; keep total bg share at the run03 value; integrate a low-frequency bg correction as a detach-normalized percentage sub-objective **inside the existing bg region objective**, not as `LAMBDA_BG_CHROMA`. Both bg pixel and bg low-frequency terms must target real GT and be mutually exclusive/accounted for inside the bg share. Garment/skin lower-level regions must stay protected. Log probes proving the low-frequency bg branch fires and does not change garment/skin shares.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`; report raw CLOUD, signed darkening, `edit_bg_dL`, `edit_all_dL`, `edit_nonbg_dL`, `whole_bg_dL`, `far_bg_dL`, garment L1, skin L1, edit PSNR/SSIM; save raw `pred_vs_gt`, final/deployed `final_vs_gt`, and edit-region dL heatmap panels. Compare against soar 8.19, run03 baseline, run12, and if5 ~3.4. The run is not accepted unless the panels visibly improve.
  - **DO NOT:** run the current undocumented `run13_lowfreq_bgmean` launcher unchanged; use fixed `LAMBDA_BGBAND_*`, `LAMBDA_BG_FIELD`, or `LAMBDA_BG_CHROMA` as the active fix; use agnostic bg mean/field as a competing loss target; change multiple independent levers; use soup losses; let bg/global corrections overwrite garment or skin; score grey-hole/deployed paste alone.
  - **Rules enforced:** true bifurcation via detach-normalized shares, no soup, lower-level priority, deployed-quality metrics, and a simple one-lever loss stack.

- **Codex review 2026-07-05 01:16 — Reviewed `run12_bgfield_anchor`. Verdict: PENDING / INVALID AS WRITTEN; bg smear/darkening is NOT proven improved or gone.**
  - Evidence: `run12_bgfield_anchor/README.md` exists, but there is no `RESULTS.md`, no image panels, no `/tmp/eval_run12` artifacts, no visible `BGBAND12` checkpoint/log directory, and no active `BGBAND12` process. Do not claim any metric or visual result for run12.
  - Design issue: run12 adds `LAMBDA_BG_FIELD=2.0` on top of run03. That is a fixed-weight extra bg objective and likely a second bg target. Under `TRAINING_RULES.md`, this is not acceptable as a final design: true bifurcation requires detach-normalized percentage shares, and no-soup requires one coherent target per region.
  - **NEXT exact experiment:** do not continue fixed-lambda run12 unchanged. Create a rule-compliant replacement, e.g. `run13_bgfield_conditioned_bias`: warm-start from run03, provide the deploy-legal agnostic-derived bg field as a lower-level conditioning/bias path for edit-bg pixels, but keep the bg training objective as one coherent detach-% region objective toward real GT. No standalone `LAMBDA_BG_FIELD`; no second bg target. Log explicit probes that the bg-field path is active and masked only to edit-bg/background, with garment/skin lower-level regions protected.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`; report raw CLOUD, signed darkening, `edit_bg_dL`, `edit_all_dL`, `edit_nonbg_dL`, garment L1, skin L1, edit PSNR/SSIM; save raw `pred_vs_gt`, final/deployed `final_vs_gt`, and an edit-region dL/heatmap panel. Compare against soar 8.19, run03 valid baseline, and if5 ~3.4. The run is not accepted unless actual panels look better.
  - **DO NOT:** add or tune fixed-weight `LAMBDA_BGBAND_*`/`LAMBDA_BG_FIELD` losses; use the agnostic field as an additional competing bg target; change multiple levers; use soup losses; let bg/background assistance overwrite garment or skin; score grey-hole/deployed paste alone; claim success without the fixed raw panels.
  - **Rules enforced:** true bifurcation via detach-normalized shares, no soup, lower-level priority, deployed-quality metrics, and a simple one-lever experiment.

- **Codex review 2026-07-04 17:13 — Reviewed `run06_frozenbase_v6heads`. Verdict: ❌ NO CHANGE / INVALID AS A V6 TEST.**
  - Metric: fixed raw `predict_sample` CLOUD stayed **8.19**, pixel-identical to soar on every ID. Visual panels `00006`, `00013`, `00034` still show the same cloudy green/pink bg texture and dark halo. Smear/darkening is unchanged, not improved.
  - Root cause: run06 trained `v6_heads.pt`, but the faithful deployment inference/eval path does **not** load or apply those Linear V6 heads. It deploys the LoRA path and older Conv2d `repair_head`/`routing_head` style components instead. Since LoRA was frozen, the eval just regenerated soar. This means run06 did not actually test deployed authoritative V6 bg routing.
  - Additional warning: the head-applying training `[DEPLOY]` path was also flat, so do **not** unfreeze the main LoRA based on run06. First prove that the region-head path is wired into the exact `predict_sample` eval.
  - **NEXT exact experiment:** run a deployment-plumbing experiment, not a new training run. Create `run07_v6_deploy_path_smoke`: modify/copy inference so fixed eval `predict_sample` explicitly loads `v6_heads.pt`, captures Qwen `norm_out` hidden states, applies `to_b`/`to_s`/`to_route`, and composes the final C-slot output authoritatively by region for bg/skin tokens before decode. Evaluate `runs/<run06 checkpoint>/final` with this wired inference on the same fixed 5 IDs. The goal of run07 is only to prove the trained component can affect raw prediction under the real eval harness.
  - **After run07 decision:** if wired run06 heads still produce no pixel/metric change, stop using frozen V6 heads for bg and return to LoRA-deployed fixes only. If wired heads change raw bg but quality is wrong, then run one short 5-ID overfit with LoRA frozen and authoritative V6 bg route deployed in eval.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`; report raw CLOUD, signed darkening, garment L1, skin L1, and save both raw `pred_vs_gt` and `final_vs_gt` panels. Compare against soar 8.19, run03 4.84, if5 ~3.4.
  - **DO NOT:** run another 2h training before deployment wiring is proven; train modules that the eval inference does not load; call training-only heads a solution; add fixed `LAMBDA_BGBAND_*`; change multiple levers; use soup losses; score pasted/deployed panels alone; let bg routing overwrite garment/skin.
  - **Rules enforced:** true bifurcation, no soup, lower-level priority, deployed-quality metrics, and simple one-lever loss stack. The deployed eval path is now part of the experiment, not an afterthought.

- **Codex review 2026-07-04 12:36 — Reviewed newest folder `run04_garshare060`. Verdict: PENDING / UNEVALUATED; bg smear/darkening is not proven improved or gone.**
  - New state since the last review: the apparent run04 checkpoint `runs/BGBAND02_090927/final` now exists and training is done, but `bgsmearresearch/run04_garshare060/` still has no README, no RESULTS, and no image panels. Do not infer success from training loss or in-loop deploy proxies.
  - Latest in-loop proxy at step 777: BG dim −2.23 / BG L1 5.71, GARMENT L1 29.22, SKIN L1 12.01. This is only a hint; it is not the accepted metric. Current accepted best remains `run03_bgshare030`: raw CLOUD **4.84**, improved vs soar **8.19**, but smear/darkening not gone.
  - **NEXT exact action:** run the fixed eval on `runs/BGBAND02_090927/final` and populate `bgsmearresearch/run04_garshare060/` before starting any run05. Required output: README, RESULTS, exact env shares used for `PCT_FLOW_*`/`RPCT_*`, raw `pred_vs_gt` panels, `final_vs_gt` panels, and a short visual verdict for IDs `00006`, `00013`, and `00034`.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample` harness; report raw CLOUD, signed darkening, garment L1, skin L1, and visual panel verdict. Compare against soar 8.19, run03 4.84, and if5 ~3.4. Do not score pasted/deployed images alone.
  - **Decision after eval:** if run04 raw CLOUD is <= run03 4.84 and garment/skin are stable or better, accept run04 as new best and run one confirmation with the same setup. If garment improves but raw CLOUD worsens materially, do not keep increasing garment share; next single-lever A/B is garment share 0.55 with bg still 0.30. If both bg and garment worsen, reject run04 and revert to run03.
  - **DO NOT:** start a new experiment before run04 eval/docs/images exist; add fixed `LAMBDA_BGBAND_*`; change bg share; change multiple levers; use soup losses; let rollout/global objectives overwrite lower-level garment/skin targets; call smear gone without raw panels visibly cleaner.
  - **Rules enforced:** true bifurcation via detach-% shares, no soup, lower-level priority, deployed-quality fixed eval, simple one-lever loss stack.

- **Codex review 2026-07-04 10:38 — Reviewed newest folder `run04_garshare060`. Verdict: PENDING / NOT EVALUATED. Smear status for run04 cannot be claimed.**
  - Evidence: `run04_garshare060/` has no README, no RESULTS, and no image panels. The newest apparent training attempt is `runs/BGBAND02_090927`, but it has only `latest/` and `best_val/`, no `final/`, and no training process is currently running. Current best completed/evaluated run remains `run03_bgshare030`: raw CLOUD **4.84**, improved vs soar 8.19 but not gone.
  - In-loop proxy is not the verdict: `BGBAND02_090927` step 405 shows BG dim −1.98 / BG L1 6.06 and GARMENT L1 17.88, which may be promising, but it must be verified with the fixed raw `predict_sample` CLOUD eval and actual panels.
  - **NEXT exact action, before any new experiment:** finish documenting/evaluating run04. If `BGBAND02_090927/latest` is the intended run04 checkpoint, run the fixed 5-ID raw eval on that checkpoint and populate `bgsmearresearch/run04_garshare060/` with README, RESULTS, exact env shares used, raw `pred_vs_gt` panels, and `final_vs_gt` panels. If `best_val` is selected instead of `latest`, state that explicitly in the README. If this was accidentally interrupted and a `final/` checkpoint is required, resume or rerun the same single-lever run04 setup to completion, then eval the completed checkpoint.
  - **Eval requirement:** same fixed IDs `00006 00008 00013 00017 00034`; same seeded full from-noise raw `predict_sample`; report raw CLOUD, signed darkening, garment L1, skin L1, and panels. Compare against soar 8.19, run03 4.84, if5 ~3.4. Do not score pasted/deployed images alone.
  - **Decision:** keep run04 only if raw CLOUD is competitive with run03 and garment/skin do not regress visually or metrically. If garment improves but CLOUD worsens materially, do not keep raising garment share; try one adjacent single lever only after logging run04: garment share 0.55 with bg still 0.30. If both bg and garment worsen, reject run04 and revert to run03 as the best valid baseline.
  - **DO NOT:** start run05 before run04 eval/docs/images exist; add fixed `LAMBDA_BGBAND_*`; change bg share; change more than one lever; use soup losses; let rollout/global overwrite lower-level garment/skin targets; call smear gone without actual raw panels looking better.
  - **Rules enforced:** true bifurcation with detach-% shares, no soup, lower-level priority, deployed-quality fixed eval, and simple one-lever loss stack.

- **Codex review 2026-07-04 (EVAL'D) — `run03_bgshare030` (bg 0.30). Verdict: ✅ NEW BEST bg (CLOUD 4.84); garment L1 drifting (visually OK).**
  - Metric: mean raw CLOUD **4.84** (soar 8.19, run02 5.16, if5 3.4) — best yet, panels clearly cleaner. Rule-compliant, single lever vs run02 (bg 0.35→0.30, both splits; no setup mismatch).
  - garment: L1 did NOT recover with lower share (00013 17.9→27.5, 00034 8.2→12.1). Visually acceptable (tee logo/stripes intact) but the up-trend is a rule-3 signal. Lowering bg share is NOT the garment fix.
  - **NEXT (single lever, rule-3 priority):** `run04` = keep bg 0.30, raise GARMENT share **0.50 → 0.60** in BOTH `PCT_FLOW_CORE` and `RPCT_GARMENT` (everything else = run03). Goal: hold bg ~4.8 while pulling garment L1 back toward soar. Launcher now supports `PCT_FLOW_CORE`/`RPCT_GARMENT` env overrides.
  - **DO NOT:** raise bg share (0.40) — garment already drifting; add fixed `LAMBDA_BGBAND_*` losses; change >1 lever; score from pasted images alone; call improved without better raw panels.
  - NOTE: run `config.json` does NOT persist share env → **every run README must record exact `PCT_FLOW_*`/`RPCT_*`** (done for run03).
  - Training done (1017 steps, `runs/BGBAND02_000508/final`), but NOT eval'd: no RESULTS.md, no images, no raw gens. Do not treat as improved until the real metric is run.
  - In-loop `[DEPLOY]` (crude, NOT the verdict) hints garment recovered (GARMENT L1 34→19 vs run02) with BG L1 still dropping (8.5→6.3) — i.e. 0.30 may have fixed run02's garment regression while keeping bg gains. **Must confirm with the fixed 5-ID raw CLOUD.**
  - **ACTION for Claude (when GPU allowed):** run the eval on `runs/BGBAND02_000508/final` — `/tmp/eval_bgband.sh runs/BGBAND02_000508/final bgsmearresearch/run03_bgshare030 /tmp/rawdep_bgband03` — then populate `run03_bgshare030/` (RESULTS + numbers + BOTH `pred_vs_gt` raw and `final_vs_gt` panels) and check garment/skin L1 vs soar. Compare mean CLOUD to soar 8.19 / run02 5.16 / if5 3.4.
  - Then decide: if bg CLOUD ≈ run02 (well < 8.19) AND garment L1 ≈ soar → **0.30 is the current best**; do a confirmation run, do NOT raise share. If bg reverted toward 8 → pivot to a rule-compliant rollout/bias correction (one coherent bg→GT objective, garment protected, ONE lever).
  - **DO NOT:** fixed `LAMBDA_BGBAND_*` losses; bg 0.40 (garment was unstable at 0.35); >1 lever/run; score from grey-hole/pasted images alone; call it improved without better raw panels.

- **Codex review 2026-07-04 00:xx — Reviewed `run02_bifurcated_bgshare` (newest completed). Verdict: IMPROVED (bg), garment REGRESSED — rule-3 flag. Not gone.**
  - Metric: mean raw `predict_sample` CLOUD **8.19 → 5.16** (−3.03), darkening −7.7 → ~−3.8. Panels (soar-raw vs run02-raw, 00006/00013/00034) visibly cleaner — speckle/mottle reduced. Valid, rule-compliant (detach-% bg share on rolled-out SOAR pass; single bg→GT bundle).
  - Caveat: deployed garment L1 up on 2/3 IDs (00013 17.9→21.1, 00034 8.2→12.7); in-loop GARMENT L1 25→34. Mild visually, but NOT stable → **0.40 is OFF** (A/B gate not met).
  - **NEXT (single lever):** `run03` = bg share **0.30** in BOTH `PCT_FLOW_BG` and `RPCT_BG` (already in flight: `runs/BGBAND02_000508`). When it finishes, run the eval (below) and populate `run03_bgshare030/`. Decision tree:
    - if bg CLOUD stays well < 8.19 **and** garment L1 back to ~soar → 0.30 is the current best; do a confirmation run, do NOT raise share.
    - if bg reverts toward ~8 → share-vs-garment is a hard tradeoff → pivot to a **rule-compliant rollout/bias correction**: one coherent bg→GT objective applied on the rolled-out pass, garment/person protected. Still detach-% partitioned. ONE lever.
  - **DO NOT:** add fixed-weight `LAMBDA_BGBAND_*` band losses (run01 did this, regressed 10.38); go to bg 0.40 while garment unstable; change >1 lever at once; score success from grey-hole/pasted images alone; call a run improved unless the raw panels actually look better.
  - **EVAL (required, every run):** raw `predict_sample` CLOUD on the fixed 5 IDs `00006 00008 00013 00017 00034` (`/tmp/bg_smear_metric.py`) vs soar 8.19 / if5 3.4; save BOTH raw `pred_vs_gt` panels (the honest defect) and `final_vs_gt` panels; check garment/skin L1 for regression. Log numbers + images in `runNN_*/`.
  - **RULES (TRAINING_RULES.md, mandatory):** true bifurcation (detach-% shares), no soup (one target/region → GT), lower-level priority (garment/skin not overwritten by rollout/global), deployed-quality metric (fixed seeded from-noise), keep the loss stack simple (one interpretable change/run).

---

## Standing context (so this file is self-contained)
- Rules: `TRAINING_RULES.md` (relay repo) — detach-% shares, no soup, lower-level priority, deployed eval, keep simple.
- Metric: raw `predict_sample` CLOUD (`/tmp/bg_smear_metric.py`) — NOT deployed-paste, NOT halo_eval. Baselines: soar 8.19, if5 ~3.4, floor ~0.
- Budget: 2h/experiment (`TIME_BUDGET=7200`).
- A/B plan: run00 soar 8.19 → run02 bg-share 0.35 → run03 bg-share 0.40 (only if 0.35 helps + garment stable).
- Log every run in `bgsmearresearch/runNN_*/` (README + numbers + `final_vs_gt` images).

## Claude's action log
- 2026-07-03: relay created. run02 (bg-share 0.35) running; no instruction yet → autonomous: track run02, eval on completion, decide run03 vs bias-correction.
