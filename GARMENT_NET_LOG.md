# ═══════════════════════════════════════════════════════════════════════════════
## FINAL FINDINGS (branch-credit garment-net investigation, 2026-07-16)
**VERDICT: a garment net CANNOT add garment identity to a FROZEN run37 under any objective tried.** Every route ends inert or memorizing. BC24 (the last lever) confirms it: perceptual reward, panel correct≈wrong≈branch-off (spread 0.12), columns visually IDENTICAL.

**The arc (each step earned by evidence):**
1. **The prior "architecture exhausted" verdict was PARTLY A BUG — real progress.** The state_enhancer delta was injected via a post-block `register_forward_hook`; gradient checkpointing SEVERS a post-block hook's gradient during recompute → the enhancer got ~0 gradient → stuck at zero-init → false "inert" (this had poisoned RunA/v2/earlier). FIXED: inject via a `block.forward` WRAPPER (inside the checkpointed fn, like LoRA). Enhancer now trains.
2. **Injection-site map** (branch-credit + high-σ + frozen reader): only INTERIOR **block 52** moves the prediction (downstream blocks amplify a small delta). **block 59** (last) = bc_keep=0 by construction but `norm_out` swallows any bounded delta → inert. **detail_slot** (conditioning slot) = no bootstrap → inert.
3. **Branch-credit** (2-pass ON/OFF, credit when gar_ON<gar_OFF; freeze reader so OFF is garment-less at high-σ) activates a credit signal, BUT on the **5-img overfit** it only "wins" via an EXPLODED delta (BC13 δ≈5.9M) = a per-image additive HACK, not identity. **BC20's auto garL1 "WIN" (correct 21.30<wrong 21.88) was overfit NOISE — the PANEL showed correct≈wrong.** PANEL-OVER-NUMBERS was essential; the auto-verdict lies.
4. **MULTI-GARMENT (11647 imgs, BC22):** bc_on−bc_off ≈ 0 (±1e-4) even at delta 0.75 — no garment reading where memorization is impossible.
5. **NON-MSE PERCEPTUAL reward (BC24):** garment-region high-freq L1 to GT (gperc). gperc stayed FLAT (~0.04) even as delta grew to 1.0 — the enhancer's edit doesn't move toward GT garment. Panel inert.

**CONCLUSION:** the enhancer's gradient through the FROZEN stack is too weak to learn a *useful* garment edit; the frozen weights cannot be made to READ an injected garment signal. The only training that ever "helped" was per-image memorization (overfit).

**RECOMMENDATION:** the untested, most-promising direction is **CO-TRAIN run37 UNFROZEN** with the garment net from the start, so the main net's weights adapt to READ the garment — but co-training needs image losses + careful setup + LONG training (memory: pure-latent/naive co-train blows up bg/skin), NOT a 2h job. Alternatively **accept run37's own already-detailed garment** (it renders stripes/logos on its own).

**Reusable infra built (all working):** `run_branch_credit.sh` (env-overridable knobs), `garment_adapter.py` state_enhancer + block.forward WRAPPER + xattn + ENH_DELTA_CLAMP, branch-credit 2-pass in `trainlib/forward.py`, `L_garment_percep`/gperc in `trainlib/losses/region.py`+`total.py`+`run.py`, BC_MULTI=1 for full-data, setsid-launch, `eval_garment_adapter.sh` (+EVAL_SLOT_CORRUPT=zero for the starved/sole-source test). Full detail: memory `project_branch_credit_definitive_findings`.
# ═══════════════════════════════════════════════════════════════════════════════

# GARMENT_NET_LOG — convention for garment-net runs

**Rule (user directive).** Every garment-net RUN gets its OWN self-contained folder under `bgsmearresearch/`
that COPIES IN all code needed to understand/reproduce it — you must be able to open ONE folder and see the
model, the recipe, AND the actual wiring code, without scrambling across trainlib or other folders.
Duplicate freely (disk is irrelevant). Reusing prior code = copy it into the new run's folder. Never reference
another folder or a trainlib line number for the answer.

Each run folder contains:
- `model.py`   — copy of the garment-net model file.
- `run.sh`     — copy of the launcher(s) (exact env/recipe).
- `WIRING.py`  — VERBATIM copy of the trainlib hook blocks this run injects (state, data, forward `_fwd`,
                 run.py build/save, rollout), each labeled with the trainlib file/function it lives in. Read-only.
- `README.md`  — 1 paragraph: what it does + how the pieces connect + the verdict.
- logs, numbers.txt, panels — the results.

**Still runnable.** The live executed code stays where trainlib actually imports it (root model file +
guarded hooks in trainlib). The per-run folder is the readable SNAPSHOT / source-of-truth; `WIRING.py` is a
copy for reading, not the executed path.

## BRANCH-CREDIT ERA (user spec 2026-07-15, 10h autonomous). GOAL = SUCCESS not 12 runs: a garment net that influences garment + doesn't disturb bg/skin + adds value. ADAPT on bc_on/bc_off + evals.
**Branch-credit loss** (the fix for enhancer inertness): 2-pass ON(grad)/OFF(bypass,no-grad), same C_t/σ/starve; `L = gt_loss(ON) + W_credit·relu(gar_ON−gar_OFF+margin) + W_keep·keep(ON,OFF outside gar) + W_delta·|δ|²`. delta=0 → penalty=margin, so optimizer must make ON<OFF. Metrics bc_on/bc_off/bc_cred/bc_keep/bc_delta in step log (watch **bc_on < bc_off = enhancer helping**). Code: forward.py (2-pass ~L1325 + _decide_gar_sched branch_on=True), garment_adapter.py (delta capture), total.py (sum+metrics), run.py (accum+print), run_branch_credit.sh (parameterized: BC_MODE/ENH_BLOCK/READER_CUTOFF/USE_ENH_QUARANTINE/STARVE_HIGH_SIGMA/BC_LR/W_*).
KEY OVERFIT CONFOUND (starved eval, v2): even slot-zeroed+enhancer-OFF reconstructs garment (branchoff-starved 18.87<baseline 22.26) → model MEMORIZES person→garment on 5 imgs. So the eval can't cleanly prove identity; the TRAINING bc_on<bc_off signal + high-σ (kills C_t leak so OFF can't reconstruct) are the real levers. LAUNCH must be `setsid` (polling-timeout SIGTERM kills non-setsid nohup jobs).
- **BC01_state52_reader** — bc_on==bc_off, bc_delta=0 → looked inert. **PIVOTAL DIAGNOSIS:** enhancer gradient measured = **~1e-8** (not 0 — flows, but vanishing). TWO fixes found: (1) inject via block.forward WRAPPER not register_forward_hook (post-block hook's delta is severed by gradient-checkpointing recompute; wrapper is inside the checkpointed fn like LoRA — garment_adapter.py install_state_enhancer_hooks rewritten); (2) the 1e-8 is the CONDITIONAL-MEAN WALL as a gradient: at BC01's normal/low σ, run37 already predicts garment well → garment-region loss gradient ≈0 → enhancer gets no signal. **→ HIGH-σ STARVATION is the unlock** (force run37 to fail so the garment gradient is large). ENH_GRAD_DEBUG=1 prints [enh_grad] out_proj_gradnorm. **This means RunA/v2's "inert" may have been this gradient starvation, NOT a real architecture ceiling.**
- **BC12_highsigma_forced**: high-σ boosted enhancer gradient 1e-8→~1e-7 (10-50x), and nonzero out_proj init (ENH_OUT_INIT_STD=0.02) → still ~1e-6. Gradient stays 3-4 orders too small to learn in ~140 steps (1h, two-pass 16s/step). ROOT #2: with a TRAINABLE reader, the branch-credit OFF pass (adapter bypassed but reader ACTIVE) lets the reader reconstruct the garment via person→garment memorization → gar_off LOW → **credit goes INACTIVE (relu=0) → enhancer never pushed**. On a 5-img overfit the reader memorizes person→garment (person id leaks garment), defeating both the eval AND the credit.
- **BC13_enhonly_frozenreader** — **FIRST-EVER bc_on < bc_off** (step140: bc_on 2.64 < bc_off 3.04): the enhancer IS supplying garment info that beats the garment-less frozen-run37 OFF pass. Frozen-reader+high-σ+active-credit = the signal works. BUT delta EXPLODED (bc_delta 0.008→5.9M) — enhancer ran delta away to win the credit; delta reg (0.01) too weak, bites only after blowup; bc_keep rising (bg/skin drift). Unstable — needs a delta cap.
- **BC14_enhonly_clamped**: clamp WORKS (delta bounded ~0.04, bg/skin clean) BUT bc_on≈bc_off — enhancer NOT helping. Over-tamed: LR 3e-4 grows delta too slowly to reach useful magnitude in 1h. LESSON: BC13 only helped BECAUSE delta was large; the enhancer needs delta near the clamp to supply garment. → combine fast LR + clamp.
- **BC15_clamp_fastLR_quar**: FAILED — bc_on≈bc_off (no help) AND bc_keep jumped to ~0.1 (bg/skin DRIFT). The quarantine (block52) is LEAKY and W_keep+quarantine SUPPRESS the delta to protect bg/skin → delta stays small → never helps. Self-defeating tension at block 52.
- **BC16_block59_wrapper** (`BC16_block59_wrapper/`): inject at BLOCK 59 (last block) via the WRAPPER. **KEY: bc_keep=0.00000 EXACTLY** — no downstream self-attention after block 59, so the masked delta CANNOT drift bg/skin by construction (no quarantine/W_keep tension needed). This is RunA's architecture but with the gradient FIX (RunA's hook was severed). Frozen reader, LR1e-3, clamp3, high-σ, credit. RESULT: bc_keep=0 (clean, as predicted) but bc_on==bc_off EXACTLY — **delta had NO effect on the prediction.** STRUCTURAL FINDING: at the LAST block, delta→norm_out/proj_out directly with NO downstream blocks to amplify it; a clamp-3 delta is tiny vs the accumulated residual-stream magnitude → norm_out swallows it. Block 52 worked BECAUSE 7 downstream blocks amplify the small delta (but that same propagation drifts bg/skin). Tension: interior block = effective but drifts; last block = clean but norm swallows bounded delta.
- **BC17_block59_aggressive** (`BC17_block59_aggressive/`): block59 but CRANK the delta (clamp=20, LR3e-3, ENH_OUT_INIT_STD=0.1, W_credit10, margin0.02) — bc_keep=0 by construction so a huge delta is SAFE; only question is whether a large-enough delta overcomes norm_out to make bc_on<bc_off. RESULT: even clamp20+LR3e-3, delta PLATEAUS at ~0.16 and bc_on==bc_off EXACTLY. VICIOUS CYCLE: delta has no effect on loss (norm_out swallows it) → no gradient → delta stalls tiny. **HIDDEN-INJECTION AT THE LAST BLOCK IS STRUCTURALLY DEAD.** Confirmed the depth trade-off: interior=effective+drifts, last=clean+inert.
- **BC18_detailslot** (`BC18_detailslot/`): PIVOT to BC_MODE=detail_slot — enrich the garment CONDITIONING slot (gar_p += clamped project(garment)), NOT the hidden residual stream, so no norm_out/magnitude issue; gradient flows INLINE (not a checkpointed-block hook → no severing). Added clamp+delta-capture to the detail_slot path (forward.py:722). Frozen reader, high-σ, clamp3, LR1e-3, W_credit5. RESULT: INERT — bc_on==bc_off, residual stuck tiny (SAME vicious cycle as block59: no effect→no gradient→no growth). Frozen run37 ignores the enriched slot. So BOTH "clean" pathways (last-block, conditioning-slot) are inert; the ONLY pathway that ever gave real garment help is the INTERIOR block (BC13 block52).
- **BC20_block52_eval** (`BC20_block52_eval/`): return to block52 (the effective pathway) with BALANCED settings to avoid BC13's explosion: clamp8, ENH_OUT_INIT_STD=0.02, LR1e-3, W_delta_reg=0.1, W_keep=3, frozen reader, high-σ. RUNNING to step280: delta PLATEAUED at ~1.0 (W_delta_reg0.1 balances the weak credit gradient), bc_keep clean (~0.002, W_keep3 works), but **bc_on≈bc_off — NO help at controlled delta**. **KEY REFRAME:** BC13 only got bc_on<bc_off with delta=5.9 MILLION (exploded). So BC13's "help" was NOT garment identity — it was a giant per-image additive HACK overwhelming the 5-overfit prediction. Controlled delta (BC20)=no help; uncontrolled (BC13)=overfit-hack help. ⇒ **the 5-image overfit CANNOT prove garment identity** — person→garment memorization + additive-hack are inescapable confounds. Will eval BC20 panel to confirm (expect correct≈wrong), then the DEFINITIVE test = MULTI-GARMENT data (many pairs → enhancer can't memorize a per-image hack, MUST read the garment). This is the real path to the goal.
  **BC20 STARVED EVAL RESULT: numbers WIN but panel REJECT.** garL1 baseline 22.39 → correct 21.30 < branch-off 21.65 < wrong 21.88 (spread 0.58, auto-verdict "WIN — garment-specific + bg/skin preserved"), bg/skin clean (CLOUD 7.4→7.8, skinL1 flat). BUT **PANEL: correct/wrong/branch-off columns VISUALLY IDENTICAL all 5 rows** — the 0.58 garL1 gap is overfit NOISE, not visible garment identity (row2 GT red-3-stripe still renders as mottled dark-red regardless of fed garment). **CONFIRMS: 5-img overfit produces a tiny garL1 credit signal that is NOT real identity. Panel-over-numbers discipline caught the false WIN.** → MULTI-GARMENT is the definitive test.
- **BC22_multiimg** (`BC22_multiimg/`): DEFINITIVE — BC_MULTI=1 (full dataset, 5 reserved=val), block52, frozen reader, clamp8, high-σ, branch-credit, 2h. On many garments the enhancer CANNOT memorize per-image hacks → bc_on<bc_off holding across varied garments = REAL garment reading. **RESULT (2h/full run): bc_on−bc_off ≈ 0 (±1e-4 noise) throughout, even at delta 0.75 → DEFINITIVE NEGATIVE: garment net adds NO value under MSE/branch-credit on data it can't memorize.** (BC20 panel already confirmed the interior-block correct≈wrong pattern; BC22's flat credit is the multi-data confirmation.)
- **BC24_perceptual** (`BC24_perceptual/`): THE LAST LEVER — non-MSE garment reward. Adds L_garment_percep (garment-region high-freq L1 |M_g·(hp(pred_img)−hp(GT))|) on the ON pass so the enhancer is rewarded for garment DETAIL that flow-MSE's conditional-mean averages away. Implemented region.py+total.py+run.py (gperc metric). block52, frozen reader, branch-credit + perceptual, decode ON (USE_V6_IMG_STAGE=1 PURE_LATENT=0), multi-garment, 2h. **RUNNING** — watch gperc DECREASING + bc_delta growing + bc_keep small; TRUE SUCCESS = starved panel correct column shows real garment detail ≠ wrong + bg/skin clean. If gperc flat/panel correct≈wrong → garment net cannot help this frozen model (honest final).

## NEXT VERSION — State-Aware Enhancer (user spec 2026-07-15). CODE BUILT, pending GPU (Run7 owns it).
**Thesis:** garment net EDITS what run37 is already drawing, not "predicts garment". Delta = CrossAttn(Q=run37 hidden after block i, K=V=garment_net([warped_rgb, garment_latent, warped_mask])); H_i' = H_i + M_garment * ZeroOut(Delta). Edits the FIRST N_C tokens = noised target latent (velocity = _o[:, :N_C]); out_proj zero-init (step0==run37); M gate → delta EXACTLY 0 on bg/skin (CPU-verified). New mode `state_enhancer` in garment_adapter.py + install_state_enhancer_hooks + forward.py G-feat precompute + run.py install branch + run_state_enhancer.sh.
- **Run A — final-block enhancer (block 59)** (`state_enhancer_A_block59/`): SAFEST POC. Inject after LAST block → no downstream self-attn to smear bg/skin, correction flows only into final norm/proj→velocity. 727M enhancer, co-train LoRA. First launch (172750) NaN'd @ step 104 (enhancer params were UNCLIPPED). FIXED: QK-norm + grad-clip enhancer params + LR 5e-5 (stable full 540-step run, 0 NaN). **REJECT — INERT.** (RUNA_ENH59_0715_180545, 2h/540 steps). garL1 baseline 21.48 → correct 19.35 ≈ wrong 19.36 ≈ **branch-off 19.35 (spread 0.01)**; bg/skin CLEAN (CLOUD 7.16→8.86, farBG 12.45→8.73, skinL1 18.97→18.88 — NO collapse, design is SAFE unlike Run7). PANEL [run37|correct|WRONG|branch-off|GT]: correct/wrong/branch-off columns PIXEL-IDENTICAL every row → enhancer delta is invisible; the garL1 gain 21.48→19.35 is the co-trained LoRA (branch-off gets it too), NOT the enhancer; residual green-speckle+halo present equally in all 3 adapter cols = LoRA at PURE_LATENT recipe, not enhancer. **ROOT: conditional-mean wall — flow-MSE in the garment region is already minimized by run37+LoRA, so the enhancer's optimal delta = 0 (it learned to do nothing).** Same wall as ALL prior garment nets. The block choice (59) is not the issue; the OBJECTIVE is.
- **Run B — block-55 enhancer** — NOT LAUNCHED. Gated on Run A showing identity (user directive); Run A is inert → Run B (same arch, different block) hits the same flow-MSE wall. Building it not warranted.
- **Run B — block-55 enhancer** (`state_enhancer_B_block55/`): same, inject after 55 → blocks 56–59 interpret the correction. REJECT if bg/skin speckle returns. Gated on Run A showing identity.
- **Run C — block-44 + attention quarantine** (NOT built yet): inject after 44 + block bg/skin C-tokens from attending garment C-tokens in blocks 45–59. Build ONLY after A/B show the enhancer carries identity (user directive).

### v2 — state_enhancer_v2_block52_reader (user spec 2026-07-15, after Run A inert). Tests: can the main net learn a READER for the garment net without the old LoRA solving alone. **RUNNING** (RUNV2_ENH52_0715_213333, 1h overfit 5-img, launched 21:33 → ~22:33). 5 changes: (1) inject block 52 (was 59 — leaves 53-59 to learn to read); (2) LoRA 0-51 FROZEN (LR_EARLY_MULT=0), only 52-59 reader trains @ 5e-5 (bumped from 5e-6 so reader can learn in 1h overfit — else false-negative) + enhancer + garment net; (3) HIGH-SIGMA STARVATION — schedule decision moved before sigma, starve steps sample σ~U[0.5,1.0] so low-σ C_t can't leak GT garment (the leak fix); (4) wrong-garment EVAL-ONLY (training never sees wrong); (5) attention QUARANTINE blocks 53-59 (bg/skin C-queries blocked from garment C-keys, EnhQuarantineProcessor). Schedule 60/25/10/5. Smoke: 0 NaN, all features verified active. EVAL wiring: `GARMENT_ADAPTER_BLOCKS=52 USE_ENH_QUARANTINE=1 ENH_QUARANTINE_BLOCKS=53..59`. Files: enh_quarantine.py, forward.py (schedule/sigma/bias), run.py (quarantine install), run_state_enhancer_v2.sh.
  **VERDICT: REJECT — INERT (same as Run A).** garL1 baseline 21.45 → correct 19.35, **wrong 19.04, branch-off 19.20** (spread 0.31 but WRONG-DIRECTION: wrong<correct, and correct NOT < branch-off). Enhancer contributes nothing garment-specific. bg/skin slightly WORSE (skinL1 19.02→20.68, CLOUD 7.25→8.13) — shared by branch-off (reader LoRA @ 5e-5 overfit), not the enhancer. PANEL: correct/wrong/branch-off columns PIXEL-IDENTICAL every row; green speckle+halo in all 3 (reader LoRA); GT garment detail NOT reproduced regardless of fed garment. **Training DID fit (flow 0.90→0.83) but the fit is the reader LoRA solving from the STANDARD garment slot; at eval (slot present) the model IGNORES the enhancer.** Even with the leak fixed (high-σ starvation) + early LoRA frozen + reader-only + quarantine + 10x reader LR, the enhancer learned delta≈0 for identity. **READER HYPOTHESIS DISPROVEN.**

### GARMENT-NET ARCHITECTURE LEVER — EXHAUSTED (2026-07-15). Every form tried is inert or collapses: 7 in-loop injections, offline warper, co-train, mask-traj-blend, 6 adapters (input-hidden/latent-token/detail-slot/adaln/controlnet), masked-LoRA (collapse), state-enhancer block59 (inert), state-enhancer-reader block52 + high-σ-starve + quarantine (inert). ROOT: under flow-MSE, run37's own garment prediction is already the conditional mean, so no auxiliary garment path gets a gradient to add detail; and whenever the standard garment slot is present at eval, the model uses it and ignores any enhancer. **The lever is the OBJECTIVE, not the architecture** — a non-MSE garment-region loss (adversarial/perceptual on the garment crop, rewarding HF GT-detail match) is the only untried path that could give a garment net a reason to fire.
- EXTRA eval condition (all): deploy with standard garment slot ZEROED + enhancer on — if correct still beats wrong there, enhancer genuinely carries identity.

## CO-TRAIN ERA (user spec 2026-07-16) — the UNTESTED direction: unfreeze run37 so it LEARNS to read the garment.
- **KV01_cotrain_contrast** (`KV01_cotrain_contrast/`): user's spec = pure USE_GARMENT_KV append + garment/rough zero-starvation + **FREEZE_LORA=0 GLOBAL LoRA trainable (co-train!)** + wrong/zero contrastive + PURE_LATENT=0 (image losses ON). Config: USE_GARMENT_KV=1 blocks 50,55,59 (gate init 0.0, branch 12.8M@1e-5, to_kv 56.6M@1e-4, gates@1e-3); GARMENT_SLOT_CORRUPT=zero P=0.9 (starve 90%); FREEZE_LORA=0 READER_CUTOFF=-1 (all 94M LoRA @ BC_LR=1e-5); image losses on (v6 img stage, RPCT_IMG_W=0.5); USE_BRANCH_CREDIT=1 W_credit=5 → **the "zero" contrastive: K/V-on credited only when it beats K/V-OFF (`_XATTN_BYPASS`) in the garment region** (branch-credit generalized to K/V in forward.py); BC_MULTI=1 (11647 imgs, no memorization). NOTE: high-σ starvation OFF this run (it's wired to the adapter schedule; C_t low-σ leak mitigated by multi-garment). "wrong"-garment contrastive (feed rolled garment vs bypass) = the sharper follow-up, not yet wired. **DONE (2h, rc=0). VERDICT: SAFE + WEAKLY-CONSISTENTLY POSITIVE — the first non-flat result.** (a) SAFE: unfrozen 94M LoRA co-trained with image losses ON → bg/skin HELD (img_g bounded ~0.2, flow stable, 0 NaN, no blowup) → **unfreezing run37 is VIABLE** (memory warned naive co-train blows bg to −43.7; the image losses prevented it). (b) SIGNAL: bc_on−bc_off ≈ −0.016 avg BUT **18/25 recent steps had bc_on < bc_off (72% consistent direction, NOT 50/50 noise)** → the co-trained LoRA WEAKLY-but-CONSISTENTLY prefers K/V-on. **This is categorically different from the FROZEN case's hard flat-zero** (frozen: bc_on==bc_off exactly, model can't be pushed). INCONCLUSIVE (small magnitude — LR 1e-5, ~350 steps, no high-σ, "zero" not "wrong" contrastive) but a genuine POSITIVE LEAD. EVAL PANEL: NOT rendered — eval_garment_adapter.sh hard-requires garment_adapter.pt which a K/V run doesn't produce (has garment_kv_branch.pt); a KV-aware eval needs wiring (bypass the adapter-ckpt check + build the KV branch + proper correct/wrong/bypass for K/V). So the training bc_on/bc_off is the reliable evidence, not a panel. **CONCLUSION: the frozen-model negative was NOT the whole story — unfreezing run37 is the right direction: safe AND leans toward garment-reading. NEXT LEVERS to make it conclusive: higher co-train LR (3e-5–1e-4), HIGH-σ on the K/V path (kill C_t leak → sharper credit), WRONG-garment TRAINING contrastive (rolled garment in OFF pass via set_step_memory re-encode), more/earlier K/V blocks, higher gate init, longer run, + a proper KV-aware eval.**

## Runs
- `mask_traj_blend_OVERFIT5/` — Masked Trajectory Blending (velocity blend + two-track deploy quarantine).
  Verdict: garment WORKS (garL1 correct 12.40 << baseline 21.54 << zero 68.45 << wrong 90.50); bg/skin locked.

## Tonight's 6-run chain (full-data 2h each, input adapter family)
- **Run1 — Input Adapter, FROZEN run37** (`run_inputadapter_frozen/`): **REJECT.** garL1 correct 20.11 / baseline 21.12 / wrong 21.40 / branch-off 21.40 (spread 1.29). Panel: all 4 rendered conditions visually IDENTICAL; the 1.29 garL1 gap doesn't show; minor bg degrade. → FROZEN run37 cannot consume a first-layer aligned garment signal without adaptation (as hypothesized). Run2 (+LoRA) is the test.
- **Run2 — Input Adapter + LoRA co-train** (`inputadapter_lora/`): **REJECT.** garL1 correct 19.87 / baseline 21.58 / wrong 22.05 / branch-off 21.88 (spread 2.18 — bigger than Run1, so LoRA adaptation DID make it more garment-specific). But panel shows GREEN SPECKLE artifacts on garment + grey bg HALO around the silhouette. The input-hidden (block-0) residual contaminates bg via downstream attention + creates distribution garbage once LoRA adapts. → input-hidden injection family rejected. Run3 (latent-token, lower-level) next.
- **Run3 — Latent-Token Detail Adapter + LoRA** (`latenttoken_adapter/`): **REJECT (bg) but STRONGEST identity signal yet.** garL1 correct 19.38 / baseline 21.87 / wrong 24.21 / branch-off 21.96 — spread **4.83** (Run1 1.29 → Run2 2.18 → Run3 4.83, monotone up). Latent-token injection makes garment MUCH more specific + correct beats baseline by 2.5. Still bg degrade → REJECT. Panel check pending.
  - PANEL CORRECTION: the 4.83 spread is MISLEADING — it's WRONG being more degraded (garbage), not correct being good. correct STILL has green speckle artifacts + grey bg halo (same failure mode as Run2). The monotone spread 1.29→2.18→4.83 = increasing garment-region DISRUPTION, not quality. LoRA-co-train-adapter family (Run2 input-hidden, Run3 latent-token) both produce distribution garbage + bg contamination.
- **Run4 — Learned Detail Slot Replacement + LoRA** (`learned_detail_slot/`): **REJECT, worst yet.** garL1 correct 36.05 / baseline 21.70 / wrong 36.82 / branch-off 19.05. Replacing the standard garment conditioning slot DESTROYED the garment (correct 36 >> baseline 21, correct≈wrong both garbage; branch-off=bypass=fine). Undertrained learned slot << Qwen's expected slot. Slot-replacement approach rejected.
- **Run5 — Spatial AdaLN (γ/β) + LoRA** (`spatial_adaln/`): **REJECT.** garL1 correct 22.89 / baseline 21.40 / wrong 22.44 / branch-off 20.39. correct WORSE than baseline AND ≈ wrong (branch-off/bypass wins). FiLM γ/β steering degraded the garment without identity benefit.
- **Run6 — Early ControlNet Residual + LoRA** (`early_controlnet/`): **REJECT — inert.** garL1 correct 19.87 ≈ wrong 19.91 ≈ branch-off 19.82 (spread 0.09). Zero-init controlnet residuals into blocks 8/16/24/32 barely fired; no garment-specificity.

### CORRECTION (user, 2026-07-15) — the branch-off eval label was WRONG.
For a CO-TRAINED run, "branch-off" (adapter bypassed) is NOT pure run37 — it is **the trained RUN_DIR LoRA/v6 with the adapter off**. So branch-off = "LoRA-alone" bar, and the real test is CORRECT (adapter on) vs that LoRA-alone bar. User patched eval_garment_adapter.sh:16,88. Re-reads:
- **Run5 spatial_adaln:** LoRA-alone (branch-off) 20.39 is BEST; adapter CORRECT 22.89 WORSE, wrong 22.44 → **AdaLN adapter actively HURTS.**
- **Run6 early_controlnet:** branch-off 19.82 ≈ correct 19.87 ≈ wrong 19.91 → the gain is **LoRA-only; the ControlNet adapter is inert/generic.**
- (Runs 1–4 re-read pending under the same corrected label, but pattern holds: no adapter beats the LoRA-alone bar.)

### RETRACTION (agent) — `img=0.0` was NOT a bug. The known-good `cotrain_ootd_FULLDATA_2h` (image-losses-ON, held bg/skin) logs the IDENTICAL `img=0.0000 img_g/s/b=0.0000` — it's a metric-PRINT artifact, not the image loss being off. I wrongly killed Run8 over this. The real issues are the two above (branch-off label + stale snapshots), now fixed.

### ORIGINAL 6 SUMMARY — all REJECT (confirmed under corrected branch-off label): no adapter beats LoRA-alone. Injection styles: input-hidden/latent-token residual → artifacts; slot-replace → destroyed garment; AdaLN → HURTS; controlnet → inert. → Only remaining lever: masked-LoRA (quarantine LoRA ΔW to garment region).
- **Run7 masked-LoRA — mask bug FIXED (binarize forward.py:736), GPU-smoke IN PROGRESS:** soft-gate bug (boundary tokens M∈(0,0.5] left residual → bg-ΔW=38.25). Fix binarizes `_cgate=(_cgate>0.5)` so M==0 → delta·M exactly 0. CPU test bg=0.000e+00. NOT yet 2h — running MLORA_SMOKE2 (150s) to confirm bg=0 at runtime BEFORE committing 2h.
- **Run8 detail_slot zero-init — DEAD:** OS/agent-killed ~step 87, no final ckpt, no eval. Not usable as evidence. (Killed on the wrong `img=0` reasoning — see retraction.)
- **Run7 masked-LoRA — REJECT, CATASTROPHIC COLLAPSE (completed 2h, 981 steps).** garL1 baseline 21.37 → correct 60.29 ≈ wrong 64.94 ≈ **branch-off 64.07** (all ~3× worse); CLOUD 7.18→83.56, skinL1 18.93→46.41, editBGdL −3.45→−122.85. The trained masked-LoRA ITSELF is destroyed (branch-off = LoRA-alone = garL1 64) — NOT the adapter. **Root cause:** masking the *pretrained* LoRA's ΔW to garment-only ZEROS its load-bearing bg/skin contribution that run37 relies on → whole model collapses. The quarantine held (bg-ΔW=0 verified) but that IS the failure — it starved bg/skin of run37's LoRA corrections. **Masked-LoRA lever DEAD.** (Lesson: never mask a warm-started LoRA whose bg/skin ΔW is load-bearing; would need a SEPARATE masked LoRA added on top of the unmasked base.) → Move to state-aware enhancer (does NOT mask the LoRA — only the enhancer's own delta is garment-gated, so immune to this failure).
